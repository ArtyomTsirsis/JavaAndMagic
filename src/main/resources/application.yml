spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/javaAndMagic
    driver-class-name: com.mysql.cj.jdbc.Driver
  liquibase:
    change-log: liquibaseChangeLog.xml