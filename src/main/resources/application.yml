spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        package-scan: com.game
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/javaAndMagic
    driver-class-name: com.mysql.cj.jdbc.Driver
  liquibase:
    change-log: liquibaseChangeLog.xml